apiVersion: v1
kind: Service
metadata:
  name: portainer
  namespace: management
spec:
  selector:
    app: portainer
  ports:
  - port: 9000
    targetPort: 9000
    name: http
  - port: 8000
    targetPort: 8000
    name: tunnel
