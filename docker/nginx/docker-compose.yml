services:
  reverse-proxy:
    image: nginx:latest
    restart: unless-stopped
    ports:
      - "80:80"
    #   - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - lan_net

networks:
  lan_net:
    external: true
