# Documentation

- `os-rebuild.md`: notes for reinstalling Ubuntu 24.04.3 LTS and importing ZFS pool (to be added).
- `operations.md`: runbooks, troubleshooting, etc.
- `testing-install.md`: how to spin up a Multipass-based Ubuntu VM on Fedora (including firewalld rules) to rehearse installers.
- `app-storage-plan.md`: shared pattern for storing app configs/data on RAID (dev and prod).
- `prod-bootstrap.md`: end-to-end steps to rebuild the NAS in production (Ubuntu + ZFS + k3s + Flux + storage).

Use this folder for human-readable guidance.
