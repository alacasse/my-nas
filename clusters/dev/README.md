# Dev Cluster

Configuration for the local k3d-based cluster.

- FluxCD `kustomization.yaml` and `gotk-components.yaml` will live here.
- Overlays should point PVCs to hostPath or lightweight storage classes suitable for development.
